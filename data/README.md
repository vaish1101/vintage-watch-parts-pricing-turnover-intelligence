# Public sample data

`sample/synthetic_inventory.csv` contains intentionally fabricated inventory identities and stock quantities. It is generated deterministically by `scripts/build_demo_data.py`.

The file is for demonstration and testing only. It is not an anonymized extract of client inventory and contains no raw marketplace records.
